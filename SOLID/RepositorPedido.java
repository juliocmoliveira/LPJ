public interface RepositorPedido {
    //Dependency Inversion Principle (DIP) = Princípio da Inversão de Dependência — Dependa de abstrações e não de implementações.

    void salvar(Pedido pedido);
}
